/** 단일 버전 원천 — serverInfo·server.json·plugin.json·package.json 이 이 값을 따른다. */
export const SERVER_NAME = "mykifrs";
export const VERSION = "0.11.2";

/**
 * 서비스명 — 모든 도구 description 의 첫 문장("<서비스명>의 <도구명> 도구는 …")에 들어간다.
 * PlayMCP 등록명과 같아야 한다.
 */
export const SERVICE_NAME = "MyKIFRS MCP";
